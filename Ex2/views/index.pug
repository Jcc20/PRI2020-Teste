extends layout

block content
 .w3-card-4
  header.w3-container.w3-red
    h1 Classes
  table(class="w3-table-all")
     tr
      th Código
      th TITULO
     each t in tips
       tr
          td
            a(href='/classes/c'+ t.codigo) #{t.codigo}
          td= t.titulo
 